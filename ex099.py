def maior(*parametros):
    valores = []
    for i in valores:
        p = str(input("digite [N] para sair do programa: "))
        if p in "Nn":
            break
        else:
            valores.append(p)


        return valores

maior(2, 10)
